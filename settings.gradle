pluginManagement {
    // Include 'plugins build' to define convention plugins.
    includeBuild('build-logic')
}

rootProject.name = 'PhotoOrganizer'
include('takeout-organizer', 'common-utils', "stapledon-auth", 'photo-server')

