pluginManagement {
    // Include 'plugins build' to define convention plugins.
    includeBuild('build-logic')
}

rootProject.name = 'foo'
include('takeout-organizer', 'common-util', "stapledon-auth", 'photo-server')